**What this changes**

**Why**

**Scoring impact**

- [ ] none — no path that can move a score was touched
- [ ] replayed a real performance before and after; final score / counts:

**Checklist**

- [ ] `ruff check`, `ruff format --check`, `mypy`, `pytest` pass
- [ ] `uv run offbook schema` re-run if `config.py` or `record.py` changed
- [ ] no audio, session records or weights added
- [ ] the invariants in DESIGN.md still hold
